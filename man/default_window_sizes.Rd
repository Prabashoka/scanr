% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scanr.R
\name{default_window_sizes}
\alias{default_window_sizes}
\title{Choose default scan window sizes}
\usage{
default_window_sizes(
  n,
  min_window = 15L,
  max_window = NULL,
  n_windows = 5L,
  seed = NULL
)
}
\arguments{
\item{n}{Number of observations in the series.}

\item{min_window}{Smallest window size in the grid.}

\item{max_window}{Optional largest window size. If \code{NULL}, uses
\code{floor(n^(2/3))}.}

\item{n_windows}{Number of distinct window sizes to sample uniformly between the lower and upper
bounds. If more sizes are requested than available integers, all available
sizes are returned.}

\item{seed}{Optional non-negative integer seed for reproducible sampling. If
\code{NULL}, the current R random-number generator state is used.}
}
\value{
A sorted integer vector of sampled window sizes.
}
\description{
Randomly samples window sizes from a discrete uniform distribution for a series
of length \code{n}. The upper
bound defaults to \code{floor(n^(2/3))} and is always capped at \code{floor(n / 2)} so
that a complete left and right window can fit around a candidate split.
}
\examples{
default_window_sizes(n = 1000L, min_window = 15L)
default_window_sizes(
  n = 1000L,
  min_window = 20L,
  max_window = 100L,
  n_windows = 9L,
  seed = 123L
)
}
