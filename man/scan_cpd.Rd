% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scanr.R
\name{scan_cpd}
\alias{scan_cpd}
\title{Detect change points in a univariate time series}
\usage{
scan_cpd(
  x,
  window_sizes = NULL,
  alpha = 0.05,
  n_boot = 400L,
  vote_threshold = 0.5,
  min_window = 15L,
  max_window = NULL,
  block_length = NULL,
  taper = c("tukey", "none"),
  tolerance = NULL,
  random_state = NULL,
  n_jobs = NULL,
  return_all = TRUE,
  change_type = c("distribution", "mean", "var"),
  eps = 1e-12,
  batch_size = 32L
)
}
\arguments{
\item{x}{Numeric vector.}

\item{window_sizes}{Optional positive integer vector of scan window sizes.}

\item{alpha}{Significance level, either as a proportion such as \code{0.05} or a
percentage such as \code{5}.}

\item{n_boot}{Number of tapered block bootstrap replications.}

\item{vote_threshold}{Minimum normalized ensemble vote score for retained
change points.}

\item{min_window}{Minimum default window size when \code{window_sizes} is \code{NULL}.}

\item{max_window}{Maximum default window size when \code{window_sizes} is \code{NULL}.}

\item{block_length}{Optional tapered block bootstrap block length.}

\item{taper}{Taper shape, either \code{"tukey"} or \code{"none"}.}

\item{tolerance}{Distance used to merge nearby candidates across windows.}

\item{random_state}{Optional non-negative integer seed.}

\item{n_jobs}{Optional number of Rust/Rayon worker threads. Defaults to 1.
Use \code{-1} for all detected cores.}

\item{return_all}{Whether to keep per-window diagnostics and raw output.}

\item{change_type}{One of \code{"distribution"}, \code{"mean"}, or \code{"var"}.}

\item{eps}{Small positive value used to avoid division by zero.}

\item{batch_size}{Bootstrap batch size used by the Rust backend.}
}
\value{
An object of class \code{scanr_result}.
}
\description{
Detect change points in a univariate time series
}
